name: Refresh Data

on:
  schedule:
    # Runs every Monday at 06:00 UTC
    - cron: "0 6 * * 1"
  workflow_dispatch: # Allow manual trigger from GitHub Actions UI

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pipeline dependencies
        run: pip install -r pipeline/requirements.txt

      # TODO: Uncomment and configure this step once pipeline/build.py is implemented (Phase 1)
      # - name: Run pipeline
      #   env:
      #     CENSUS_API_KEY: ${{ secrets.CENSUS_API_KEY }}
      #     ZILLOW_DATA_URL: ${{ secrets.ZILLOW_DATA_URL }}
      #   run: python -m pipeline.build

      # TODO: Uncomment once the pipeline produces real output files
      # - name: Commit updated data
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"
      #     git add data/
      #     git diff --cached --quiet || git commit -m "chore: refresh data [skip ci]"
      #     git push
